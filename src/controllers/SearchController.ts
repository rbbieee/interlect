import { Preference } from "../models/Preference";
import { University } from "../models/University";

export class SearchController {
  searchUniversity(preference: Preference): University[] {
    return [];
  }

  filterUniversities(preferences: Preference[]): University[] {
    return [];
  }
}
