import { ChatHistory } from "../models/ChatHistory";

export class AIController {
  getResponse(input: string): string {
    return "";
  }

  analyzeConversation(history: ChatHistory[]): string {
    return "";
  }
}
